{
  "name": "skillbridge-functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "migrate:counts": "node scripts/migrate-counts.js",
    "test:emulator": "firebase emulators:exec --config firebase.test.json --project demo-skillbridge --only firestore \"node --test integration/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
